# PursueGen Site

This repository now includes a WordPress-native deployment target under:

`wordpress-plugin/pursuegen-leader-dashboard/`

Use this flow for production on pursuegen.com:

```bash
npm install
npm run build:wp
```

Then zip and upload the plugin folder in WordPress.

See plugin docs:
`wordpress-plugin/pursuegen-leader-dashboard/README.md`
